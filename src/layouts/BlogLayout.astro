---
const { title, description, pubDate, author } = Astro.props;
---

<article>
  <header>
    <h1>{title}</h1>
    <p>{description}</p>
    <p><small>By {author} on {new Date(pubDate).toLocaleDateString()}</small></p>
  </header>
  <slot />
</article> 